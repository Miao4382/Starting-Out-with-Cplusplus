The mystring class 
Task:
1. Dynamically allocate memory to store string
2. C-string may be assigned to mystring object with the = operator
3. one C-string or mystring object can be concatnated to another with the += operator, + operator 
4. can access individual element stored in string by [] operator, add a bound check
5. can be print on screen by << operator 
6. can use std::cin to input string to the mystring object by >> operator 
7. contains member function similar with string class
	1) string.append()
	2) string assign()
	3) string.back() & string.front()
	//4) string.begin()
	5) string.c_str()
	6) string.capacity()
	7) string.size()
	8) string.clear()
	9) string.compare(str2) (str2 can be C++ string, C-string, mystring)
	10) string.copy(str2, x, n), str2 is C-string
	11) string.empty()
	12) string.erase(x,n)
	13) string.find()
	14) string.insert()
	15) string.replace(x,n,str2)
	16) string.resize(n,'z')
	17) string substr(x,n)
	18) string.swap(str2)
8. can be compared by relational operators: ==, >, <, !=
9. initialization

//work flow:
finish:

